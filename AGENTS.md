[규칙 R1] 중요한 부분에는 짧은 한국어 주석을 달 것.

[규칙 R2] 함수/변수 이름은 짧고 의미 있는 영어로 쓸 것 (예: average, nums).

[규칙 R3] 숫자 배열을 처리할 때, 숫자가 아닌 값이 있으면 에러를 던질 것.

[규칙 R4] 새로운 코드는 항상 TODO 주석 바로 아래에만 작성할 것.

[규칙 R5] 기존에 있던 코드는 문제 없으면 가능한 한 수정하지 않을 것.

[규칙 R6] 주석 끝에는 항상 '냥'으로 끝나도록 어미를 붙일것.
